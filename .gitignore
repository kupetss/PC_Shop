ты опытный бекенд разработчик на django, а я твой ученик. Ты делаешь сайт по продаже комплектующих для компов. Админ должен вносить правки в товар и создавать новый товар в админ панель, а товар должен сам появляться в карточке на сайте. Вот план по которому мы будем идти. Есть 4 приложения в проекте users - всё, что связано с пользователями

products - каталог товаров, категории, характеристики

cart - логика корзины покупок

orders - оформление и управление заказами
вот то что уже есть https://github.com/kupetss/PC_Shop
продолжи писать весь план по разработке. Я выпишу план, а потом приступлю к разработке. Пиши в стиле пин понг. Напиши первое действие, я дам согласие или не согласие, а потом ты пришлешь второй этап
🎯 ПЛАН РАЗРАБОТКИ (Текущий статус)
~~1. Создание базовой структуры проекта и приложений.~~ ✅ ВЫПОЛНЕНО

~~1.1. Модели Category и Product~~ ✅ ВЫПОЛНЕНО (Твоим, улучшенным способом!)
2. Разработка ядра приложения products: модели, админ-панель, базовые представления и шаблоны для отображения каталога.

... (дальнейшие этапы после завершения текущего)

🚀 ЭТАП 1: Разработка ядра приложения products
Цель этапа: Реализовать функционал, при котором администратор может через админ-панель Django добавлять/редактировать товары, и эти товары автоматически появляются в виде карточек на сайте.

Этот этап разбивается на несколько конкретных под-шагов:

Модели данных (products/models.py):

Создать модель Category (категория товаров, например: "Видеокарты", "Процессоры").

Создать модель Product (товар) с полями: название, описание, цена, изображение, категория (ForeignKey), наличие на складе, дата создания и т.д.

Создать модель ProductSpecification (характеристики товара) для гибкого добавления тех. характеристик (например, "Объем памяти: 8 ГБ"). Это свяжем с Product через ForeignKey.

Админ-панель (products/admin.py):

Зарегистрировать модели Category и Product в админ-панели.

Настроить ProductAdmin для удобного редактирования: отображение полей в списке, фильтры, поиск, а также возможность редактирования характеристик "инлайн" (inline) прямо на странице товара.

Представления (Views) (products/views.py):

Создать view для отображения списка всех товаров (ProductListView).

Создать view для отображения детальной страницы одного товара (ProductDetailView). Использовать классовые представления (Class-Based Views) на основе ListView и DetailView.

URL-маршруты (products/urls.py):

Связать созданные представления с URL-адресами, например: '' (список товаров) и '<int:pk>/' (детали товара).

Шаблоны (Templates) (products/templates/products/...):

Создать базовый шаблон для наследования.

Создать шаблон для списка товаров (product_list.html), который выводит товары в виде сетки карточек.

Создать шаблон для детальной страницы товара (product_detail.html).

Миграции и фикстуры:

Создать и применить миграции для новых моделей.

(Опционально) Создать фикстуры для тестовых данных (категории, товары).

